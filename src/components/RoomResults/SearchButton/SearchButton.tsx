import Button from '@mui/material/Button';
import { StyledButton } from './SearchButtonStyles';

const SearchButton = () => {
  return (
    <StyledButton variant="contained">Search Dates</StyledButton>
  )
};

export default SearchButton;
